import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1
  },
  text: {
    fontSize: 14,
    textAlign: 'center',
    color: '#525467',
    fontStyle: 'italic',
  },
});
