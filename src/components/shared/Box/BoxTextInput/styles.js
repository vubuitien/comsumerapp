import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderColor: '#B33B16',
    borderWidth: 1,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 5,
    overflow: 'hidden'
  }
});
